name: Ping mi bot

on:
  workflow_dispatch:   # ← esto permite ejecutar el workflow manualmente
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hacer ping a mi bot
        run: curl https:/
